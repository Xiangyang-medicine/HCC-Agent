# Supplementary Figure S4

Run with the locked project environment:

1. `python prepare_source_data.py`
2. Run each panel script.
3. `python assemble_supp_figure_s4.py`
4. `python verify_supp_figure_s4.py`

Every panel folder contains `source_data.csv`, plotting code, and standalone
SVG/PDF/PNG/TIFF outputs. The combined figure is exported in the same formats
at the figure root.
